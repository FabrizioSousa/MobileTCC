<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PrimeiraVersao.Views.Login"
             xmlns:vm="clr-namespace:PrimeiraVersao.ViewModels"
             Title="{Binding Title}">

    <ContentPage.Content>

        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
            <Label Text="CresceDin" FontSize="50" FontAttributes="Bold" HorizontalTextAlignment="Center">
            </Label>
            <Label Text="E-mail" FontSize="30" FontAttributes="Bold"/>
            <Entry x:Name="TxtEmail">
            </Entry>
            <Label Text="Senha" FontSize="30" FontAttributes="Bold"/>
            <Entry x:Name="TxtSenha" IsPassword="true">
            </Entry>
            <Button x:Name="btnLogin" VerticalOptions="Center" Text="Login" Clicked="btnLogin_Clicked"/>
            <Button x:Name="bnEsqueciSenha" Clicked="bnEsqueciSenha_Clicked" Text="Esqueci minha senha" BackgroundColor="LightBlue" />
            <Button x:Name="bnCadastro" Clicked="bnCadastro_Clicked" Text="Cadastrar usuário" BackgroundColor="LightBlue" />

        </StackLayout>
    </ContentPage.Content>

</ContentPage>
